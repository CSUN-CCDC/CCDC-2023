
# /etc/login.defs file validity tester
## Sample python script for checking compliance of /etc/login.defs

### Sample Usage
`$ python3 init.py`

On failure, returns the number of checks failed.

`$ echo $?`


` 
