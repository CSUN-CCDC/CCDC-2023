# etc/passwd validity tester
## Python script that verifies users in /etc/passwd against your database of users

### Sample Usage
`$ python3 src/init.py`

On failure, returns the number of users failed.

`$ echo $?`


## Manage the database of users within init.py
