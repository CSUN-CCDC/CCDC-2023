---
- name: Playbook for testing firewall related roles
  hosts: all

  roles:
    - role: "../roles/facts"
    - role: "../roles/ufw"
      vars:
        ufw_lockdown: true
    - role: "../roles/ufw"
      vars:
        ufw_open_ports: [80,443]
